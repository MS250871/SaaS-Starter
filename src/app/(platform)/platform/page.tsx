import { DestinationPlaceholder } from '@/components/layout/destination-placeholder';

export default function PlatformPage() {
  return (
    <DestinationPlaceholder
      route="/platform"
      title="Platform Dashboard Placeholder"
      description="This is a temporary landing page for platform users after post-login. We can replace it with the real platform dashboard next."
    />
  );
}
