import { WorkspaceModulePlaceholder } from "@/modules/workspace/components/workspace-admin-dashboard"

export default function WorkspaceSettingsPage() {
  return (
    <WorkspaceModulePlaceholder
      title="Settings"
      description="Choose a settings area from the sidebar to manage profile, theme, access, features, or audit controls."
    />
  )
}
