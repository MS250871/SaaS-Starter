import { DestinationPlaceholder } from '@/components/layout/destination-placeholder';

export default function SelectWorkspacePage() {
  return (
    <DestinationPlaceholder
      route="/select-workspace"
      title="Choose Workspace Placeholder"
      description="This is a temporary landing page for users who belong to multiple workspaces. We can replace it with a proper workspace switcher next."
    />
  );
}
